# 按发言人筛选

雷达、收藏列表中的「发言人」按钮打开可搜索的选择面板，支持姓名和 @账号搜索。选择一个发言人后，只显示此账号的消息；点击右侧 × 或选择「全部发言人」清除。与主题、来源、关键词、仅重点、排序及收藏条件叠加。

- `/v1/articles?author=x:handle:openai` 按平台与规范化账号精确匹配，在分页/计数前过滤。无 handle 的文章按平台与原始作者名称匹配（如 `rss:name:研究员: 小明`）。同名不同账号、不同平台、正文提及或引用对象不混入。
- `/v1/article-authors` 与列表共用 `article_query()`。返回当前条件下全部匹配数据的作者及数量，不限于 App 已加载的第一页，也不限关注名单。展示名采用该账号最新一条匹配文章中的名字。
- 普通雷达沿用最近七天窗口、搜索及低信息量准入规则；收藏沿用已收藏历史记录的范围。筛选仅改变读取条件，不触发采集、翻译或模型调用。
- App 查询与离线缓存带独立作者参数；切换作者从第一页开始。选择面板支持空结果、失败重试和离线标记。一次选择一个账号；关闭 App 后筛选状态与其他现有列表筛选一样重置。

Android 内容版本 `0.13.0.12`，沿用 runtime `0.13.0`；不新增依赖、权限或原生模块。iOS 共用界面实现，当前仍无经过原生验证的 iOS 发布基线。

## 2026-09-19 交付验证

- 后端筛选、准入、时效回归 73 项通过；App 筛选/内容展示/缓存回归 18 项及更新状态相关回归通过；TypeScript、Ruff、diff 检查通过。
- 本地窄屏共享界面实测：打开面板、搜索 @账号、选中后仅显示该作者、清除恢复全部，布局正常。Android Hermes 导出及前后原生 fingerprint 均匹配基础包。此次未完成安卓模拟器原生界面操作验收（桌面 UI 工具无法识别其窗口），不据此宣称用户手机已经应用；iOS 未发布新包或 OTA。
- 生产公网 API 实测 64 位发言人/195 条匹配消息的快照；OpenAI 6 条、Boris Cherny 9 条、Elon Musk 28 条分别精确匹配，完整分页及收藏数量一致，匿名请求 401。
- 服务端实现 `e33f62c15511e846e9dd30c06f283de19efd2d4b`，部署源清单 SHA256 `cf4c8f9f0438c8ef6bf214e626505f66e7875d11a2af96688df7c1306362427d`。原文、译文和审核表未修改，其他服务保持原进程。
- Android stable `0.13.0.12` / runtime `0.13.0`；release `d8077a43-0ebe-4d1d-8bb6-025e5f304d2f`，update `14074716-ebf4-4fac-ab13-242e8baeb116`。preview 与 stable 的公网清单签名及两个资源哈希均验证通过。设置 → 应用更新 → 检查更新 → 立即应用更新后可用。
