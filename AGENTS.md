# AI Radar 开发约定

继承上级工作区规则。用户已明确开发与内容处理的职责边界：

- 开发者负责代码、配置和验证。文章采集、翻译、整合、总结及纠错由服务器统一流程执行。
- 发现翻译问题时，读取原文与审计记录定位原因，修复通用逻辑并补充必要回归，再让服务器处理。不能直接改数据库文章或译文、手工设置审核通过、注入某篇文章的目标译文，或写一次性内容修补脚本。
- 历史翻译复核使用正式 `radar translate --recheck-article ARTICLE_ID` 或 `radar translate --recheck-editorial`。这些命令只指定已有记录范围，不接受替换文案。
- 不通过删除问题、放宽门槛或反复无变化抽审来制造通过结果。保留原文、候选历史与模型审计；未通过的候选不发布，已通过缓存复用。
- 只读比较候选、查看问题和验证模型输出属于开发诊断，不等于人工编辑内容。验收应区分自动化测试、服务器真实调用与尚未完成的外部授权。
