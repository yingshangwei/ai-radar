# 行业研究验收记录

2026-09-13，功能源码提交 `50c12983fd686e4058bb20d3d56325394b30ab6d`。实现与限制见 [行业研究](INDUSTRY.md)，来源接入范围见 [免费来源](INDUSTRY-SOURCES.md)，采购参考见 [费用表](FINANCIAL-DATA-PRICING.md)。

## 自动化检查

- 后端完整套件 1,682 passed、18 skipped；跳过项沿用既有外部/可选条件，另有一条既有 Starlette 弃用提示。
- 最后增加「采集整轮完成后再冻结报告」控制，关联服务、API、队列和迁移回归 42 passed。
- 行业 schema 与现有模型路由联合回归 21 passed，使用本地假 Codex 子进程，未调用真实模型。验证 strict schema、生成/修订与审核路由、最多两次底层调用、审核回执复用和升级条件。
- App 完整测试 65 passed、1 项既有 skipped；TypeScript、Expo web export、后端 Ruff 和 diff 检查通过。
- 关键回归包括历史证据版本、禁止正文降级、未来时间排除、引用校验、审核后发布、单次修订、重启回执恢复、二次审核未知调用、每日阶段预算、认证和暂停、免费采集与旧流水线隔离。

## 免费来源真实采集

先独立核验 17 家 SEC CIK 与返回的证券代码一致，再使用正式 `IndustryService.collect` 在隔离 SQLite 执行一次完整采集：检查 22 来源，22 来源成功，新增 101 份证据版本，模型阶段 0。三个主题分别匹配 75、62、51 份证据，可跨主题重复计入；不能把三个数量相加当成不同文章总数。

实测 NVIDIA、AMD 可取得部分官方公告正文。SEC submissions 可用，但 Archives 存在 403；Broadcom 正文请求存在超时。正常的索引/摘要采集不等于全文全部成功，具体按每条证据的 `partial` 与范围核对。该次采集未生成模型报告，不能将默认「证据不足」当成模型已完成的产业结论。

本地实测原始记录留在 Git 忽略目录 `dist/industry-research/`：`source-probe.json`、`issuer-probe.json`、`free-collection.json` 及测试日志。

## 界面和预发布

390 × 844 视口验证行业切换、空主题、暂停保留旧报告、历史引用、约 60 段长原文滚动与关闭、预算/来源说明、底部导航及采集 running → completed。控制台无错误；此项使用明确标记的本地模拟资料，截图不作为真实产业判断证据。

Android 内容版 `0.13.0.3` 沿用 `0.13.0` 原生 runtime。预发布更新 ID `d4ca559f-d8d7-469b-b385-ac065313eaae`，经过原生指纹、RSA 签名和两个资源哈希验证。构建使用项目 pnpm 10.11.0；运行时附带的较新 pnpm 曾尝试自动检查依赖，已改回项目版本，未变更依赖锁文件或原生配置。

## 服务器迁移预验收

腾讯云 staging invocation `inv-h8jmq6gc5c` 完成：核对 13 个修改的后端源文件和当前生产 manifest；从生产数据库做一致副本，在副本执行正式初始化，证明原 22 张表的所有行和 schema 保持不变，只新增 3 张行业研究表，模型调用 0。

## 生产启用与 Android 验收

激活 `inv-j8jmtbgdtd`、复验 `inv-j8jmu3gg0h` 均 SUCCESS/0。生产 manifest 为 `faecf64ae307e337909447c506834a863528ef3b298a5bf72030560cd963a4cd`，绑定上述源码提交。实际迁移前后旧 22 表完全一致；启动后复验 315 篇既有原文、382 份已通过译文、32 个关注配置和 14 个既有来源配置保留。新增 22 个行业来源、3 个跟踪主题；环境文件和其他服务保持一致，健康检查及行业接口认证正常。

生产首轮检查 22 个来源，新增 151 份证据版本（含复用的既有免费材料），0 个来源检查失败。手动重复检查复用同一个已完成任务，未绕过来源间隔。三个主题的首份报告均由真实服务器模型完成并通过审核，总共使用 8 个处理阶段；其中算力与基础设施经历了一次自动修订后再审。公网核对三份报告的 17 个不同引用均可打开固定证据版本，首次取得时间和发布时间都不晚于报告截至时间。未人工编辑报告或修改审核结果。三份报告当前均判断材料不足以支持整个行业改善或走弱；这属于已完成的研究结论，不是生成失败。真实报告只证明该次自动流程可完成，不证明行业结论或投资表现永远正确。

Android stable 发布 `inv-j8jmukgrei` SUCCESS/0，公网签名、两个资源哈希和 runtime 再次通过。专用 Android 模拟器从 0.13.0.2 实际下载并应用 0.13.0.3，连接保留，行业页显示上述生产数据，报告引用可打开对应的历史原文版本；关闭网络后冷启动仍为 0.13.0.3，之后已恢复网络。行业报告本轮只做内存缓存，不宣称重启后可离线读取报告。原生验收记录为 `dist/industry-research/native-verification.json`，实际行业页截图为 `android-live.png`。

本轮没有新建原生包，也没有发布 iOS OTA。兼容 Android 0.13.0 基础包可从设置检查并应用更新；更早原生版本须先覆盖安装已有 0.13.0 基础包。
