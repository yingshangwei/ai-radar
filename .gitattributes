* text=auto
*.bat text eol=crlf whitespace=cr-at-eol
# Keep the public certificate byte-identical to the embedded/native baseline and server copy.
app/updates/certificate.pem -text whitespace=cr-at-eol
