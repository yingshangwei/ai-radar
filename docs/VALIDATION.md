# 验证记录 · 2026-09-08

## 当前服务器版本与验收范围

- 当前 release **20260908-07015b7** 已由 TAT `inv-m8d69c0sm9` 成功启用（SUCCESS，退出 0）。完整测试 **732 passed、6 skipped**，Ruff 通过；6 项为既有浏览器环境限制。归档 128,176 字节、48 份已提交文件、6 个分片，SHA-256 `8b15b4094c0b766d739e778799334d4d67901a5d08a1cc9296264b3e8269a5e5`。实际归档逐文件与提交一致，新增数字模块在包内，分片及嵌入的源码清单均核验一致。
- 新校验覆盖 Jan / Mar 等月份缩写的上下文、中文数字列表与合法千分位、中文邻接 7B 等数量单位、负号和版本号；百分比支持 percent / per cent / 百分之 / 全角％对应，正文 @账号使用 ASCII 边界，URL 中账号由完整链接检查。回归同时保留真实数字、单位、账号和链接差异的拒绝，独立语义审计及修订轮次未放宽。详见 [翻译机制](TRANSLATION.md)。
- 最终部署前只读预览 `inv-e8d66s0kah` 于 **2026-09-07 21:52:54.642617 UTC** 成功。范围为 47 份绑定网页及主消息对应的 93 份缓存、277 个段落，其中 97 个未完成段落的数字问题 **43 → 31**、账号问题 **2 → 0**；全部检查新增问题数为 **0**，96 个当前策略已审计段落前后均无机器检查问题。实际输出 `dist/cloud/number-context-preview-adf7df523500.txt` / `.json`。检查仅在内存运行规则，没有模型调用、内容写入或状态重标；不能把规则变化当成候选获得语义批准。
- 预检 `inv-n8d6870h30` 生成一致性备份 `radar-translation-number-context-20260907T215422Z.db`；激活结果 `dist/cloud/translation-number-context-activation.txt` 确认 14 张表、三份当前配置、Caddy / 原 8080 及九组历史均保持。新增第八组为已结束的阶段验证操作，第九组为阶段配置指针、收据、旧配置 / 数据库备份及收据指定的受保护文件；当前配置 SHA 与已应用的收据一致，旧七组证据未重复注入或改写。
- 本次没有运行模型或重审内容，部署后主消息仍为 **46 ready**，47 份关联网页全文仍为 **13 ready、34 review_required、0 error**，全文中文尚未完成。API、浏览器 worker 与 Caddy 正常，余额告警为空；此前待审内容未重新尝试。**2026-09-07 21:57:56.910998 UTC** 公网只读 `dist/cloud/translation-number-context-public.json` 通过：默认近七天 42 篇消息及 43 个关联资源（13 ready、30 review_required），可见范围与数据库全量分开统计；未通过全文全部隐藏，无令牌请求 401、reader 管理请求 403，无运行任务或授权窗口，余额告警为空、调度器开启，X 和官方来源 healthy、Facebook auth_required。Android / iOS 源码未变，无需重新构建或安装。

## 历史：4d196af 分阶段配置与真实调用

- 当时 release **20260908-4d196af** 已由 TAT `inv-m8d4nk0pmn` 成功启用（SUCCESS，退出 0）。完整测试 **594 passed、6 skipped**，Ruff 通过；6 项为既有浏览器环境限制。127,211 字节归档包含 47 份文件，实际只改变 CLI、配置和翻译三个运行时模块。预检 `inv-j8d4mgg5fd` 生成 `radar-translation-stage-options-20260907T210017Z.db`。
- 本次代码部署保持 14 张表、三份私有配置、Caddy、原 8080 服务及七组历史证据。第七组保护上次已经结束的 `c10b0ead` 操作；部署本身不启动模型。实际启用结果保存为 `dist/cloud/translation-stage-options-activation.txt`。
- 新能力按初稿、修订、独立审计分别配置请求选项和 token 预算；阶段对象完整替换公共对象。真实 SDK 模拟请求验证三阶段参数、并发隔离、嵌套配置保护、重试预算、保活数据下的整体超时、空或截断输出拒绝，以及脱敏日志。`--translation-diagnostics` 仅开启翻译 logger 的 stderr 诊断，stdout JSON 不变，结束后恢复日志配置。
- 随后配置切换 `inv-n8d4v4g0xk` 成功：仅七处叶字段变化，校对与审计分别启用 `thinking=enabled`、`reasoning_effort=high`、32768 token，整体请求时限 300 秒；初稿仍 disabled / 12000。原配置备份及严格差异记录位于 `/var/lib/ai-radar/translation-stage-config/20260907T210825Z-e965fb23/metadata.json`，实际结果 `dist/cloud/translation-stage-config-applied.txt`。14 张表、两个凭据文件、七组历史、浏览器/Caddy/8080 服务保留，API PID 1848549，调度器保持开启；配置操作没有调用模型。
- 正式单条 errors-only 验证由 `inv-m8d5020e3h` 启动，任务 `fafcf476-8167-4070-ba94-41d7da07fa82` 于 2026-09-07 21:11:36.638942 UTC 开始。它使用当前保存的 [Google 官方公开文章](https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-agentic-video-in-gemini/)及机器候选，服务器统一处理，不接受替换文字或人工批准。21:15:14 UTC 只读观察 `inv-m8d53gg577` 确认一条 correction 正常结束，用时 171123 毫秒，输入 3641 / 输出 15923 / 推理 14725 token；当时整篇仍在运行，不能把一次调用成功视为内容通过。缓存键、已通过部分复用、原文与审计历史保护及未通过不发布的门槛均保留。
- 上述任务于 **21:18:13.821015 UTC completed**，唯一 error 缓存转为 `review_required`，没有新增整篇 ready。4 次 DeepSeek Pro 调用（校对、审计各 2 次）均 `finish_reason=stop`，推理 token 分别为 14725、9990、4363、5016；模型协议调用完成和内容通过是不同结论，不继续重复运行此已结束任务。
- 最终只读 `inv-e8d57ignvx` 于 21:19:34.807318 UTC SUCCESS，实际输出与对应 JSON 为 `dist/cloud/translation-stage-validation-final.txt` / `.json`，元数据 `/var/lib/ai-radar/translation-stage-validation/20260907T211134948319Z-e0f4258a/metadata.json`。60 份既有 ready、34 份未选待审缓存（33 份关联正文、1 份历史）、所有未选译文、原文等 11 张表、配置及七组历史均保持；单元 exited / 退出 0，无运行任务、租约或授权窗口。`verification_passed=true` 仅证明结束和保护核验，`translation_ready=false`。
- 最终全量主消息 **46 ready**，47 份关联网页全文为 **13 ready、34 review_required、0 error**，全文中文仍未全部完成。21:19:44 UTC 公网只读 `dist/cloud/translation-stage-public.json` 核验默认近七天 42 篇消息与 43 个资源（13 ready、30 review_required）；所有未通过正文均不暴露，无令牌读取 401、reader 管理请求 403，余额告警为空，X/官方源 healthy，Facebook auth_required，调度器开启。
- Android / iOS 源码未变，无需重新安装。Apple 下载账号访问仍等待已发出的授权回复；此前审批对单条 DeepSeek 调用的拒绝已通过只读公开来源核实消除：该内容来自 Google 官方公开文章、不含已配置凭据，按用户既有明确翻译授权重新审批后才启动。没有绕过审批或手工改文案。

## 历史：f574f87 部署与字面量恢复

- 当时 release **20260908-f574f87** 已由 TAT `inv-m8d3qqgvhd` 成功启用（SUCCESS，退出 0）。完整服务器测试 **506 passed、6 skipped**，Ruff 通过；6 项跳过均为既有浏览器集成环境限制。125,817 字节的归档包含 47 份已提交文件、6 个传输分片，路径与 SHA-256 见 [部署记录](DEPLOYMENT.md)。
- 预检 `inv-e8d3pbg22j` 生成一致性备份 `radar-translation-calendar-context-20260907T202807Z.db`；部署前后 14 张表、三份私有配置、Caddy 与原 8080 服务保留。六组历史证据均不变，第六组包含已经结束的 errors-only 操作之 latest、metadata、数据库备份与私有输出；新部署使用独立前缀，不覆盖旧证据。
- `cae1e18` 将受保护字面量错误恢复与 JSON/Schema 恢复共用最多两次请求预算，同一原文与输入草稿不变，只反馈安全对应错误，不拼接缺失链接或人工改文案。`f574f87` 补齐通用月份上下文规则，避免从小数/版本末尾误取日期；中英货币等价仅在适当语境适用，重量和明确人名不被误当作币种或月份。独立语义审计、真实缺漏与未解决疑点的发布门槛保留。
- `cae1e18` 曾通过 `inv-e8d3b6g244` 部署（484 passed、6 skipped、Ruff 通过），但只读影响检查发现三个新数字标记，因此先完成通用修复再升级。`inv-m8d3n2gncq` 在内存检查 34 份当前网页缓存中的 98 个未完成段落，确认新增日期误报消除：旧/新数字标记均为 43，货币标记 3 → 2，链接校验保留。该检查没有模型调用或数据库写入，不代表其余数字或语义疑点已解决。
- 正式 errors-only 启动 `inv-m8d3sj08ug` 成功，任务 `c10b0ead-2174-4239-bd61-53294353a8ce` 于 2026-09-07 20:31:38.714907–20:32:03.448959 UTC completed，只处理 1 份现存 error 缓存，结果仍为 **error / correction / protected_literal_mismatch**。未继续重试、未改文章内容、未人工批准，也没有新增发布成功的整篇译文。
- 最终只读验收 `inv-m8d3t8g1sa` 于 20:32:18 UTC 成功，实际输出及同一输出提取的 JSON 为 `dist/cloud/translation-calendar-literal-final.txt` / `.json`。`verification_passed=true` 表示操作结束及保护核验通过，**translation_ready=false**；systemd 单元 exited、退出 0。60 份既有 ready 缓存、34 份未选待审缓存（33 份关联正文、1 份历史缓存）、11 张受保护表、配置、原文、旧补译基线和旧 `bccb692e` 操作均不变。
- 独立元数据为 `/var/lib/ai-radar/translation-calendar-literal-recovery/20260907T203137040137Z-6b927236/metadata.json`。数据库全量主消息 **46 ready**；47 份已保存关联网页正文仍为 **13 ready、33 review_required、1 error**，全文中文尚未补齐。
- 20:33:24.632861 UTC 公网只读验证 `dist/cloud/translation-integrity-public.json` 成功：状态总消息数 46，默认近七天可见 42 篇消息与 43 个关联资源，资源为 **13 ready、29 review_required、1 error**；可见范围不等于数据库全量。未通过的全文中文不暴露，无令牌读取 401、reader 管理操作 403，无运行中任务或授权窗口，余额告警为空，调度器开启，X 与官方信息源 healthy、Facebook auth_required。
- Android / iOS 源码未变，本次没有重建安装包。本机 iOS 运行仍缺完整 Xcode 与模拟器运行时，Apple 下载登录访问被自动审批审查拒绝，用户访问授权请求尚待回复；没有绕过授权或生成真机 IPA。免费获取 Xcode 与付费 Ad Hoc 分发条件分开说明，见 [IOS.md](IOS.md)。

## 历史：37e24fc 部署与补译验收

- 当时 release `20260908-37e24fc` 已由 TAT `inv-e8d1wbgit4` 成功启用（SUCCESS，退出 0）。完整服务器测试 **386 passed、6 skipped**，Ruff 通过；6 项跳过均为既有浏览器集成环境限制。源码包 123,815 字节、47 份已提交文件，SHA-256 `d2993b78cec1fb3338eca0cd37a34254ba092741494fec292d458ac2cc2458e0`，见 [部署记录](DEPLOYMENT.md)。
- 部署前后 14 张表保留；补译操作另核对原文、摘要等 11 张受保护表、58 份既有 ready 缓存、未选择缓存和三份私有配置。Caddy、原 8080 服务以及五组历史诊断记录和基线保留。
- 正式补译任务 `61afa513-2046-4730-9200-9f9d9e209144`（提交 `inv-j8d18t05tr`）于 2026-09-07 19:04:23–19:24:50.928733 UTC 执行完成。原补译操作第 2 轮选择 36 份缓存，结果 2 ready、26 review_required、8 error；全体网页 13 ready、26 review_required、8 error，主消息 46 ready。19:25:54 UTC 只读验收 `inv-j8d1v0g2tu` 成功，原始基线、11 张受保护表、58 份既有 ready 缓存、未选缓存、配置和原文不变；部署基线 9 份缓存中已有的 14 个通过段落均保留。任务 completed 不能代表全部通过。
- `37e24fc` 预检 `inv-e8d1vs0h3p` 成功，已生成 `radar-translation-stage-recovery-20260907T192644Z.db` 一致性备份，启用 `inv-e8d1wbgit4` 随后成功。正式 errors-only 启动 `inv-n8d1xi0t6e` 成功，任务 `bccb692e-9199-4316-a347-c567e223fd68` 于 2026-09-07 19:28:41–19:40:29.942517 UTC 执行完成，只选 8 份错误缓存，结果 7 review_required、1 error；原有 26 份网页待审不重跑。
- 19:29:20 UTC 的只读进展 `inv-e8d207gpge` 保留为历史。最终验收 `inv-m8d2bx03js` 于 19:41:51 UTC 成功：60 份既有 ready 缓存、27 份未选待审记录（26 份网页、1 份其他历史记录）、11 张受保护表、配置、原文和原补译基线不变。`verification_passed=true` 表示操作结束及保护核验通过，`translation_ready=false`，不能解释为翻译通过。任务、租约和授权窗口均为 0，单元 exited、退出 0。独立元数据为 `/var/lib/ai-radar/translation-errors-recovery/20260907T192839351351Z-7c824b11/metadata.json`。
- 最终固定验收输出保存于 `dist/cloud/translation-errors-recovery-final.txt` 与 `dist/cloud/translation-errors-recovery-final.json`，是对同一 `inv-m8d2bx03js` 的只读重取；没有因此新增模型调用、任务或测试。
- 最终数据库全量：主消息 **46 ready**，47 份已保存网页正文 **13 ready、33 review_required、1 error**。本轮最终未留下 Schema / ID 协议错误；唯一失败仍为校对阶段 `protected_literal_mismatch`，原文保护校验没有放宽，未人工改稿、批准或继续重试。
- 19:41:58 UTC 公网只读验证 `dist/cloud/web-translation-public.json` 成功：状态总消息数 46，默认近七天可见 42 篇消息及 43 个关联资源，资源计数 **13 ready、29 review_required、1 error**。这与数据库全量不是同一范围。未授权读取 401、reader 管理请求 403，未审核正文 `text_zh` 不暴露；余额告警为空，X healthy、Facebook auth_required。
- Android / iOS 源码未变，本次没有重建安装包。Facebook 账号与 Graph API 接入、iOS 实际运行和真机签名仍为外部待办，详见本文末尾。

## 历史：关联网页中文队列与失败诊断

- 只读盘点发现 47 份已保存的关联网页均有解读，但只有 11 份全文中文 ready；其余 36 份待校对或发生错误，其中 34 份已耗尽旧版重试次数。主消息的 46 条中文 ready 不能代表网页全文完成。
- `05b9c33` 将仍有关联、当前正文非空的网页纳入统一翻译队列，主消息优先、内容去重、过滤过期与孤立缓存，并在状态及任务消息中独立报告 `resource_counts`。11 项新增回归通过，完整服务器测试 286 通过、6 项既有浏览器测试跳过；没有修改移动端或依赖。
- 已隔离部署 `20260908-05b9c33`，TAT `inv-j8cwsx0a6r` 成功。正式 API 任务 `f6680ca9-6032-4bc3-889c-0584c2815062` 于北京时间 01:49:42 至 01:55:36 使用已保存原文和草稿补译；结束时网页仍为 11 ready、34 error、2 review_required。任务 completed 表示执行结束，不表示这些译文均通过。
- 只读核验 `inv-e8cx9wgqv5` 确认原文、摘要等 11 张受保护表、58 份既有 ready 缓存、其他未选缓存及三份私有配置保持不变。基线备份为 `/var/lib/ai-radar/web-translation-backlog/20260907T174942371095Z-73ba914e/before.db`。尚未完成的翻译不发布；旧通用错误无法证明是网络故障，需要服务器错误分类后再定位。
- `a27c135` 增加安全的阶段、异常类、HTTP 状态和自有验证错误分类，以及正式 `translate --limit 1 --force`。真实单条诊断定位到 `audit_part_mismatch`，没有据此假定网络或授权故障；日志不输出原始异常、原文、候选或密钥。
- `3fbd99d` 对多段返回 ID 缺失、重复或未知的结果按原 ID 单独重新审计，不重命名输出、不复用不明确的批准；每个通过段落立即保存，后续任务记住单段策略。真实任务 `1d812657`（ID 前缀）于 2026-09-07 18:19:56 UTC 结束，3 段中 2 段通过并持久保存，剩余段因 `output_schema_invalid` 保持未完成。
- `959d586` 增加仅含白名单类型和字段位置的结构诊断，以及至多一次结构修复请求；原文与候选不变，语义否决不进入格式重试。真实诊断确认 `too_long` 位于 `audits.0.issues`：模型列出的疑点超过旧版 12 条限制，结构重试后仍超过限制。任务 `157a3e92`（ID 前缀）于 2026-09-07 18:37:01–18:37:35 UTC 执行；验收 `inv-98d0gi0qh2` 确认既有 2 个通过段落复用、原文和未选缓存及 58 份 ready 缓存保持不变。
- `34a57f1` 移除校对与审计 `issues` 数组任意的 12 条上限，保留完整字符串疑点，供服务器修正和历史核对。任一未解决疑点仍拒绝发布；两轮修订、独立审计、输出最多 180,000 字符 / `max_tokens=12000`、每次最多 30 个输出段及严格 ID 校验均保留。状态摘要最多展示 30 条疑点，完整疑点留在段落及历史中；不会截断证据后批准译文。
- 历史 `34a57f1` 单条续审：启动 `inv-n8d0tcgh6c`，任务 `02be5af3-107d-42f0-ab3c-e1b1946d5b1d` 为 completed，只读验收 `inv-e8d0uigww3` 成功。本轮未记录审计协议失败，两个已通过段落复用，整篇仍为 review_required；当时网页计数 11 ready、33 error、3 review_required，主消息 46 ready。任务结束和部分段落通过不能当作整篇或全部网页完成。
- `7ab2ed6` 修订通用审计与校对指令：`issues` 应列出当前候选有依据的未解决差错，正确、等义、已覆盖或核实无误的检查笔记不应混入；输出前须核对原文与候选，消除自相矛盾判断。修订阶段把 `checks` 作为待核实线索，不能根据错误意见添加原文没有的信息。没有按关键词删除返回疑点，没有人工指定译文或修改批准结果，实际未解决疑点仍阻止发布。该版原 36 份缓存的一次正式恢复已结束，结果见本文开头。
- `7ab2ed6` 一轮结束时的 8 份错误有实际分类证据：7 份初稿响应缺少 `translations.0.approved`，1 份校对因 `protected_literal_mismatch` 被拒绝。这些历史错误没有被标成翻译成功，也没有被笼统归因为网络故障。
- `37e24fc` 将有界结构恢复提取为初稿、校对和审计共用能力：只有 JSON/Schema 不合格时至多反馈一次，反馈只含安全字段位置、错误类型和自有 schema；输入原文与候选不变，不猜测缺失的批准结果。明确审校否决不触发格式重试，ID/占位符错误仍拒绝，401/402 保持原有行为。新增 errors-only 正式 CLI 在排队与领取租约时都要求现存 error 状态，当前绑定与资格检查先于数量限制，不修改缺失绑定、待审和 ready 缓存。新格式回归 21 项与错误范围回归均通过，完整结果为 386 passed、6 skipped；该版真实 errors-only 一次恢复已结束，7 份进入待审，1 份原文保护校验错误保留，结果详见本文开头。

## 历史：服务器翻译纠错与独立审计首次验收

- 用户明确：开发者只修改代码，不能直接调整文章、译文或手工批准。已落实通用翻译流程 `85e07c5` 与正式 CLI `5672e2d`：缓存初稿 → 模型修订 → 独立语义审计 → 有界修正并复审。审计仅接收原文和候选，不接收历史编辑批准；审核绑定精确原文/候选指纹。重审入口只接受已有记录 ID 或历史范围，不接受替换文字。
- 服务器完整测试 **275 通过、6 跳过**，Ruff 和差异检查通过。跳过项为需要特定 Chromium / noVNC 路径的既有浏览器集成测试；本次未改变浏览器或移动端源码。新增覆盖包括实质错译拒绝与自动修正、漏译、金额保留、审核指纹失效、失败阶段恢复、402、租约及重审幂等。
- 新 release `20260908-5672e2d` 通过隔离部署和数据保留验证。真实 `radar translate --recheck-editorial` 首轮任务 `c7cec4c5-0dda-4dee-a84b-4a89f5627baa` 处理 4 份缓存，3 份通过，1 份网页因疑点保留待审；TAT `inv-e8cw2w0efu` 的退出 1 准确表达未完成，没有强制发布。
- 正式 CLI 续审任务 `f4b016d3-2563-412f-a50c-c6f365a89083` 仅选择剩余 1 份，TAT `inv-e8cw5jgp7k` 成功。服务器依据已保存问题实际补译了遗漏术语，修订与独立审计均通过；两个先前通过的段落逐项不变，没有重新审核。期间未改变提示词、质量门槛、模型配置或输入人工目标文案。
- 只读核验 `inv-m8cw6x0ur6`、`inv-m8cw8v021b` 确认 **4 份缓存、6 个段落**全部具备当前 `zh-independent-audit-v1` 审计，发布内容与审核指纹一致。原始文章全行、其他翻译全行及私有配置不变；旧编辑内容与来源完整留在历史中，活动结果使用配置的机器模型来源。
- 再次执行相同 CLI 的任务 `44ded4e4-9a29-480a-aaa6-06fa5079b13a` 选择 0 条，未调用模型；`inv-j8cwa30g3v` 验证全部翻译行与执行前一致。证据汇总 `dist/cloud/server-translation-evidence.json`，以及 `server-translation-verify_all.txt`、`server-translation-verify_resume.txt` 和 `server-translation-verify_noop.txt`。旧人工编辑脚本已移至 `dist/cloud/retired-manual-edits/`，仅保留历史，不再执行。
- 北京时间 01:20 公开 HTTPS 复验：46 条消息中文 ready、18 个完整采集网站、无余额告警、无授权窗口或运行中任务；X 与六个官方来源 healthy，Facebook 仍待授权。手机读取的两条相关 X 内容已使用服务器模型审核来源，原文指纹不变。本次不需重装 App。这证明当前流程与样本运行通过，不代表模型翻译永远无误，也不代表全部旧缓存已重审或全部关联网页已翻译。

## 定时运行与采集、翻译可靠性补充

- 已观察到自然定时任务 `5aa266ff-6126-450f-b65c-8e3aa288b8ca`，北京时间 9 月 8 日 00:32:19 开始、00:33:23 完成；此前同一 API 进程于 22:32:19 启动，观察期间没有手动创建采集任务。X 读取 30 条并新增 2 条，总消息数从 44 到 46；另处理 14 个直接来源并完成 1 份网页解读。证据为 `dist/cloud/goal-scheduler-before.txt`、`goal-scheduler-after.txt` 与 `scheduled-collection-observed.json`。这是实际定时执行证据，不能据此声称 X 搜索完整覆盖。
- Facebook 后续分页失败原本会丢弃已经读取的帖子，现保留有效内容及明确失败状态；13 项新增回归覆盖授权、限流、HTTP、超时、跨 Page 和实际 Pipeline 入库。
- 线上 Tibo 译文确认被 `first → 第一个`、`dollar → 美元` 两处等价表达误拦截。修复货币词和有对应英文原词的序数词处理，25 项新增回归包含线上完整原文/候选，以及金额、币种、单位、遗漏数字与错误序数仍被拒绝。相关社交测试 34 项、翻译与余额测试 44 项通过，Ruff 通过；没有更改依赖或移动端源码。
- 两份源码补丁 `338dbb9` 已部署，TAT `inv-e8cuvr0qej` 成功，七张表、三份私有配置、浏览器/Caddy/原 8080 服务均保持不变。只重启本项目 API，具体版本与备份见 [部署记录](DEPLOYMENT.md)。

## 0.5.0 自动公开读取与一次手机许可

- 服务端最终 **209 tests passed**，Ruff / TypeScript 通过；手机 **13 项回归通过**，覆盖前后台、许可撤回、同页完成验证、表单排除和源地址绑定。既有已生成译文缓存继续复用。
- 当时服务器 `20260907-622e107` 已部署。44 条消息、既有七张表的数据及三份私有配置保持不变；Caddy 与原 8080 服务保持运行。17 个已经完整采集正文的网站显示完成，不再假报未授权。当前版本见 [部署记录](DEPLOYMENT.md)。
- 云端真实浏览器对从未创建过授权记录的 example.org 自动读取成功，4.42 秒取得 112 字符，自动启用且没有人工验证时间戳。该验证使用临时数据库，不改生产正文和网站授权。
- 手机采集队列支持按许可域名查询和排除设备冷却文档；真实 OpenAI 队列排除前三篇后继续返回后续文章，查询本身不改数据库。
- Android 原生隔离验收：23:07:52、23:07:54、23:07:56 连续三次手机导入 200。只许可 example.org 一次，之后没有逐篇采集/提交操作；三篇不同 query 的绑定目标全部保存 129 字符，method 均为 mobile_browser，三个 read 任务完成。服务器 WebsiteAccess 保持未授权；fixture 禁止真实模型和服务器网络，因此本测试不证明翻译质量。证据 `dist/device-reading-ui/final-before.json`、`final-complete.json`、`actions.jsonl` 及截图。
- 修复两项仅原生暴露的问题：React Native URL 的 hash/protocol 没有 setter；Android WebMessageListener 给出的 event URL 为 origin。最终校验同时要求实际导航完整地址、文章载荷地址与 nonce 对应，未放宽成只验证域名。
- 两端最终安装产物使用源码 `98c3847`。Android 原签名、四 ABI、正式 HTTPS 配置和私密值扫描通过；隔离测试包与正式包 Hermes 完全相同。iOS 模拟器构建 `9526a508-77d8-4893-b1ff-38cac07ed0fd` 的双架构、严格签名及秘密扫描通过，未实际运行 iOS 或生成真机 IPA。产物见 [ANDROID.md](ANDROID.md) 和 [IOS.md](IOS.md)。
- 不保证第三方网站永远不再验证；手机许可持久保存，真正的登录失效或挑战才再次提示。手机读取使用设备网络并仅在 App 前台执行；服务器定时采集保持运行。未自动处理任何真实 CAPTCHA。


## 0.4.0 网页授权灰屏修复

- 修复提交 `9904dc7`。通过真实 noVNC 1.7.0 模块复现：当可选 VideoDecoder 检测的 `flush()` 永不完成时，原版模块无法加载，应用上游补丁后可正常加载。另覆盖检测 API 缺失和检测拒绝；这些故障均不会再阻止连接。
- 全套 **96 tests passed**（含 6 个实际 Chromium 浏览器测试），Ruff 与安装脚本语法检查通过；保留 1 条已有 Starlette/AnyIO 弃用警告。浏览器测试需设置 `RADAR_TEST_CHROMIUM` 为 Chromium 可执行文件、`RADAR_TEST_NOVNC` 为同时含原版 `core/` 和修复版 `android-compat-v1/` 的资源目录，否则这 6 项会跳过。新增请求超时、模块加载超时、画面握手超时，以及模块加载前先兑换票据并能正常关闭窗口的检查。
- 腾讯云静态补丁部署 `inv-j8cjsggepe` 成功；保留 release `20260907-4ac1dde` 和旧资源备份，新增独立资源路径清除旧模块缓存影响。线上资源逐字节匹配已审查源码；安装脚本以后会自动生成同一补丁。无需更换 APK 或重启服务。
- 已用现有 Android 0.4.0 / 5 安装包在 API 36 模拟器打开正式 HTTPS 授权窗口：状态「已连接」，能看到 OpenAI 网站的实际 Cloudflare 验证画面；未代做验证码。截图 `dist/browser-ui/gray-fix-native-window.png`。尚未收到用户手机型号，因此不能据此断言用户真机已验证。
- 对正式 HTTPS 页面注入 `VideoDecoder.flush()` 永不返回故障，仍在 **4.88 秒**内连接并收到非空远程画面，完成按钮启用；截图显示实际 Anthropic 文章。证据 `dist/browser-ui/gray-fix-live.json` / `.png`。保留生产 CSP；未触发正文补采或模型调用。测试仅关闭本次创建的窗口，模拟器已停止。
- 最后云端审计 `inv-j8ck1q0vu2` 成功：43 条原始消息、中文关联、54 份既有就绪译文逐项不变，配置和模型环境文件哈希不变，余额告警为空；健康检查 200、未授权 401、阅读令牌管理权限 403。浏览器健康且无活动窗口。API PID 1690875、浏览器 PID 1690965、Caddy PID 1370572 和原有 8080 服务 PID 303258 均未变化。

## 0.4.0 App 内网页授权

- Python 全套 **90 tests passed**，Ruff / TypeScript 通过，锁定依赖审计未发现已知漏洞。新增覆盖管理权限、单次票据重放、固定 Origin、Cookie 属性、过期会话、验证失败不启用、不刷新已验证文章、验证后排队补采、仅已授权域名降级、失效暂停，以及公网代理地址固定与私网拒绝。
- 腾讯云 release `20260907-4ac1dde` 已部署，静态网页尺寸修正 `dc92e48` 经原文件及新文件 SHA-256 校验后原子替换，旧资源备份保留。浏览器使用独立系统用户及进程，Chromium 沙箱开启；API 18473、浏览器 18475 与临时 VNC 端口均只监听本机。Caddy PID 1370572 和原有 8080 服务 PID 303258 保留；主配置与模型密钥文件哈希一致。
- 真正启动了云端 headful Chromium 和 VNC，读取 Anthropic 正文 25,023 字符；以 example.org 的测试 Cookie 验证关闭并重开浏览器后资料仍保留，测试 Cookie 随后清除。这验证持久化机制，不代表已登录所有网站。
- Android 0.4.0 / 5 原签名覆盖安装保留阅读连接与单独保存的管理授权。真实 HTTPS 页面打开、手机 Gboard 字符进入远程 Chromium 地址栏、键盘弹出后的操作按钮，以及「验证并补采」返回授权中心均通过。截图位于本机 `dist/browser-ui/`。额外通过 Android WebView 检查键盘展开及收起后的 footer 均在可见高度内、所有按钮的实际命中区域一致。远程显示与模拟器布局存在响应延迟；不能把点击后的即时截图当成操作已完成。
- App 完成按钮实际创建 read 任务 `853b5d80-a704-48b9-8d0b-cd9512956521`，于 18:31:52 北京时间完成：处理 12 个直接来源，新增 1 份网页解读。alignment.anthropic.com 显示 `ready`、启用后续补采，目标正文已持久化。部署前后的 **43 条原始消息、其中文关联以及 54 份已有就绪译文**逐项一致，余额告警为空。
- Android APK 69,398,978 字节、同一签名证书；iOS 模拟器构建 `46d82cca-0c07-4491-ba0a-1d40b77dbd1b` 成功，两个最终包的签名、已知 API 凭据及签名密码扫描通过，产物见 [ANDROID.md](ANDROID.md) / [IOS.md](IOS.md)。iOS 尚无实际运行或 iPhone 签名验证。
- OpenAI 部分页面仍会要求 Cloudflare 验证；未自动处理验证码，也未把 19 个尚缺正文的来源标成成功。用户可从 App 操作网站验证，服务仅在实际取得原文章正文后开启补采。Facebook 账号审核及 Graph API 权限仍需后续完成。

## 0.3.0 网页与直接资源解读

- 全套 **83 tests passed**，Ruff / TypeScript 通过。新增真实解析器、私网和混合 DNS 拦截、IP 固定与 TLS SNI、逐跳重校验、不携带 Cookie / 鉴权头、robots / 体积限制、固定一层、X URL 别名、304 / 正文变更 / 跨消息缓存、失败状态、只读 API 不调用模型，以及显式重试只恢复未完成译文的测试。pypdf 6.17.0 锁定依赖审计未发现已知漏洞。
- 通过 X 官方 Lookup 补齐 14 条旧帖的 16 个链接元数据；只更新独立引用表，原文、时间、互动、收藏和原有 40 条中文字段均未改动。两轮补读完成 **23 份真实 Codex 网页解读**。网页受限、无法读取及待处理状态保留，不能据此声称全部网页均已读完。
- 真实样本检查了 WeatherNext 3 的正文、方法与指标，Anthropic 的形式化论文 / 仓库，以及 Tibo 明确链接的 Squad 应用。当时曾直接人工调整 WeatherNext 译文，历史原因保存在服务端 `reading-weather-editorial.json`。用户已明确否定这种开发方式：后续只修复服务器通用逻辑，再由服务器重审，旧编辑记录仅作为历史审计材料保留，不能当作当前自动翻译准确性的证据。
- 真实 Codex 重建 2026-09-04 日报：11 条输入、6 条报道。验证时禁止网页读取和翻译请求，前后 **23 份解读和 63 份翻译**逐项一致；生成成功说明日报实际复用持久化内容。此缓存证明发生在单篇人工修正前，修正没有掩盖额外调用。
- Android **0.3.0 / 4** 原签名覆盖安装保留登录，实际云端中文解读、要点、关注价值、正文展开与中英切换通过。断网冷启动重新打开缓存的 Meta 文章后，解读及双语正文仍可读并标注离线。恢复网络后的正式连接正常，无 AndroidRuntime / ReactNativeJS 错误；已停止本次专用模拟器，保留原有本地开发服务。产物见 [ANDROID.md](ANDROID.md)，截图与记录位于本机 `dist/reading-ui/`。
- iOS **0.3.0 / 4** EAS 模拟器构建成功，架构、签名和秘密扫描通过；没有实际 iOS 运行 / iPhone 签名验证，见 [IOS.md](IOS.md)。
- 最终公网核验：61 个关联资源中，23 份解读已就绪、26 个仍待后续任务处理、11 个受访问限制、1 个无法读取。已取正文的全文中文状态为 5 个已就绪、11 个待校对、7 个失败待重试；这些状态不是成功覆盖。原有 40 条消息及其中文版本全部保持不变。
- 手机只读取已生成内容；点击管理任务或定时任务才启动采集与补读。网页全文若未通过校对，界面显示明确状态及原文，不能把已有中文摘要当作全文翻译已完成。


## 0.2.1 余额不足提示

- 服务端全套测试 **62 passed**，Ruff 与 TypeScript 通过。新增真实 SDK HTTP 402 故障注入，覆盖持久告警、批次暂停、重启后保留、草稿恢复、不耗尽内容重试次数、成功后恢复、并发旧成功不清除新错误，以及 401/429/500 不误报。
- 腾讯云 release `20260907-52fd8e5` 部署成功。升级前 SQLite 一致性备份完成；私有配置哈希、调度器、Codex 登录和原有服务均保留。公网逐项核验原有 **40 条内容及中文字段完全不变**，健康检查 200，`translation.alert = null`。未向生产注入余额错误。
- Android 0.2.1 / code 3，四 ABI、API 24+、同一私有 RSA 3072 签名，v2 验签与秘密扫描通过。交付包 `dist/ai-radar-0.2.1-android.apk`，68,519,253 字节，SHA-256 `5a2714df4e8d8be376554b5f927f8623203923fb463b2c3ce00aeb711bdff59d`。
- Android API 36 原生故障测试使用独立本地数据库与假密钥，经真实 SDK 注入 402。首页、设置及文章详情均显示告警，已有中文文章可读。本地测试包仅临时允许 HTTP 访问模拟器宿主，Hermes bundle 与交付 APK 的 SHA-256 完全相同；测试包不交付，生产 manifest 已还原。
- 模拟调用成功后，前台轮询自动清除详情页告警，无须重启或重新登录。再次注入 402 后，切回前台立即刷新；断网冷启动仍显示缓存告警，明确标注“上次记录”及当前离线。测试结束后模拟器网络已恢复。截图与 XML 保存在本机 `dist/billing-ui/`。
- 最终交付 APK 已重新覆盖测试包并连接正式 HTTPS 服务，首页正常、无余额告警，AndroidRuntime / ReactNativeJS 错误日志为空。仅供本次测试的本地 API 与模拟器已停止；原有本地开发实例继续保留。
- iOS 0.2.1 / 3 EAS 模拟器构建 `bab56d1f-a0a9-4df2-b8f4-a2e39e11821f` 成功，源码 `52fd8e5`；包标识、版本、最低 iOS 15.1、arm64/x86_64 模拟器架构、Hermes、秘密扫描与 codesign 校验通过。尚未实际运行 iOS App，也不是 iPhone IPA，见 [IOS.md](IOS.md)。

## 0.2.0 中英阅读验收

- Python 全套测试 **55 passed**，Ruff 与 TypeScript 通过。新增持久化缓存、跨记录去重、互动更新不重译、原文变化失效、并发租约、失败草稿续传、错误输出拒绝、数值单位校验、引用元信息保护、辅助翻译故障回退、旧数据库与中文 API 搜索/引用测试。
- DeepSeek 密钥先经官方 `/models` 验证，再以一次性 RSA OAEP/SHA-256 加密通过 TAT 安装，0600 环境文件与实际服务进程加载均验证。密钥未写入移动应用、Git 或命令日志。
- 当时服务器 release `20260907-ccd71d0`。两次隔离升级分别先备份 SQLite，保留旧 release；40 条原文、互动指标、收藏、X 参数 10/1、调度器、Codex 授权、Caddy 和原有 8080 服务均保留。
- 真实 DeepSeek Flash 翻译 + Pro 单独校对已完成 **40/40**；首次拦下 7 条，后续修订校对通过。编辑复核另修正一处主客体错误，保留前译文与修正理由，并准确记录额外复核来源。不能据此声称自动翻译零错误。
- 真实 Codex 重建 9 月 6 日日报（任务 `0cad0cbb-4dab-4e67-abea-c03a785011c7`，约 24 秒）：5 条输入合并 1 条报道、4 个中英引用。数据库逐行比较确认 **40 份翻译的 attempts 和 updated_at 完全不变**，日报复用已有译文。
- 公网 API 核验所有 40 条 `title_zh/text_zh`、中文搜索命中、全部原文/URL/时间/互动/收藏不变，日报引用 ID 与时间窗口正确，未授权仍为 401。结果保存在 `dist/cloud/translation-final-verification.json`。
- Android 0.2.0 / code 2，同一私有 RSA 3072 签名，v2 签名和秘密扫描通过；API 36 ARM64 模拟器覆盖安装后保持登录。中文列表、中文日期、原文切换、离线冷启动后的中文与原文阅读通过；测试后恢复网络，AndroidRuntime / ReactNativeJS 错误日志为空。
- Android 交付文件 `dist/ai-radar-0.2.0-android.apk`，68,518,141 字节，SHA-256 `92cae5e51a0758ab20672461f0ea9cd6eea0d64c1e80b286d7bf8b8bb39ffdb4`。截图与离线 XML 在本机 `dist/android-v0.2-*`。
- iOS 0.2.0 / 2 云端模拟器构建 `bc0c6444-eeea-42e7-95c1-ccce0b6e358e` 成功，源码 `d9004ec`。真机签名与实际 iOS 运行的限制仍见下文。

## 历史：初版与早期接入验证

| 验证 | 结果 |
| --- | --- |
| Python 服务端测试 | 43 passed；覆盖鉴权、导入、去重、筛选、收藏、时区/DST、无数据日报、授权缺失、限流、分页、X 长帖与引用上下文、分页中途额度/限流/HTTP 错误时保留已有数据并标注未完成、原文域名限制、恶意重定向、引用校验、模型适配、部署回滚，以及 Codex 原生 bundle 的完整性、目录安全与幂等安装 |
| Ruff | 通过 |
| TypeScript | `tsc --noEmit` 通过 |
| Expo 依赖匹配 | `expo install --check` 通过 |
| 原生工程生成 | Android / iOS prebuild 通过 |
| Expo 云构建授权 | CLI 回调登录成功，独立账号查询确认 `yswdra`；项目已关联，免费套餐本月有 15 次 iOS 构建额度，未升级付费套餐 |
| iOS 构建上传 | 官方 `build:inspect` 检查 72 个工作文件及 158 个浅克隆 Git 对象，设备令牌和私有签名材料未上传 |
| iOS 原生编译 | 两次 EAS 云构建成功；最终 `e54ecfe1-a025-4f8d-bb96-a208411ee7ea` 已包含直接声明的 expo-font。云端 Doctor 为 19/20，唯一提醒是保留原生工程需手动同步 app.json，本次 prebuild 差异已审查 |
| iOS 模拟器产物 | 已下载并保存 `.app` 与压缩包；应用标识、0.1.0/1、iOS 15.1+、arm64/x86_64 模拟器架构、Hermes、秘密扫描与 codesign 校验通过；尚无原生运行验证，不是 iPhone IPA |
| Android Release 安装包 | 四 ABI、API 24+、私有 RSA 3072 签名，APK Signature v2 校验通过；内置 Hermes，无需 Metro；设备令牌和签名密码未进入包内 |
| Android 实际运行 | Android 16/API 36 ARM64 模拟器安装启动成功；连接腾讯云、当期与历史日报、文章详情、收藏写入和取消、收藏列表均验证；云端确认测试收藏已恢复原状态 |
| Android 更新与离线 | 覆盖安装后登录保留；关闭模拟器 Wi-Fi/数据并冷启动，日报与信息流缓存可读且明确显示离线提示；网络已恢复，未发现 AndroidRuntime/ReactNativeJS 致命错误 |
| 三平台 JS 产物 | Android Hermes、iOS Hermes、Web export 全部通过 |
| 依赖审计 | Python 锁文件 `pip-audit` 与 App `pnpm audit --prod` 均未发现已知漏洞 |
| 真实官方采集 | 5 个官方来源已读取，收录近一周 23 条有效信息 |
| 真实 Codex 账号调用 | 已用本机 ChatGPT 登录执行 `codex exec`，完成结构化中文摘要与引用校验 |
| 真实日报 | 已生成 2026-09-04 历史日报，后续用官方正文补充证据后重新生成；2026-09-06 初次为明确标识的无新增/来源覆盖报告；补入 X 原文后已更新为真实模型日报 |
| 浏览器端到端 | 本机服务连接、真实信息流、详情、收藏写入和列表、刷新保留登录、历史日报选择已通过 |
| 视觉检查 | 已检查手机尺寸下连接页、日报和导航，以及实际历史日报；纸张白/墨黑/朱橙配色，原生轨道图形与 App 图标 |
| 部署脚本 | bash 语法检查通过；在临时目录用模拟系统命令验证正常升级、依赖安装失败、启动失败、健康检查失败、首次安装失败以及拒绝原地升级；已生成不含秘密的 server 部署包。这些测试不代替 Linux 主机实际部署 |
| 腾讯云盘点 | 已通过官方 TAT 读取目标主机端口、服务和资源；现有业务监听 8080 |
| 腾讯云真实部署 | API 与官方 Caddy 服务已运行；HTTPS 健康检查 200，未授权 401，reader 正常读取 200、管理操作 403；原有 8080 进程未变 |
| 腾讯云隔离升级 | 新 release `20260907-d600c7e` 已成功激活，独立虚拟环境和来源代码校验通过；旧 release 保留，升级前数据库一致性备份完成；33 条记录、开启的调度器、Codex 登录以及现有配置/凭据均保留 |
| 新版本云端复验 | 真实采集任务完成并新增 1 条官方信息，总计 34 条；6 个官方来源健康，原有日报可读取，HTTPS/鉴权与原有 8080 进程仍正常 |
| 补齐历史来源后的日报 | 9 月 4 日日报已用新增 X 材料重新生成：11 条输入归并为 6 条报道，引用 10 个来源，事件覆盖与所有引用的日期窗口核验通过；GPT-6 Astra 的同一发布事件优先引用更完整的官方正文，不强制重复引用 X 公告 |
| 云端真实采集 | 5 个官方来源完成首次采集，收录 23 条信息；通过公网 HTTPS + reader token 从本机读取成功 |
| Linux Codex 安装与授权 | 0.153.3 完整官方原生 bundle 经 SHA-512 与包清单校验，实际执行 `codex --version` 通过；9 月 7 日设备授权成功，独立服务用户的 `codex login status` 返回已登录 ChatGPT |
| 云端真实模型日报 | 管理 API 触发 2026-09-04 历史日报，Codex 任务在约 57 秒内完成；公网读取验证 6 条中文总结、9 个真实来源，所有引用 ID 均匹配原文。该历史回放确实调用模型，不能与无新增报告混淆 |
| 云端调度配置 | 每天北京时间 08:00 汇报、每两小时采集。已观察到 9 月 8 日 00:32 自然定时采集，X 新增 2 条，总消息数 44 → 46；具体任务与证据见本文开头 |
| Meta 官方新闻源 | 9 月 7 日 00:52 云端采集完成，Meta Newsroom 读取 10 条并接受 1 条符合时效/AI 筛选的消息；本轮另新增 1 条 OpenAI 消息，总计 25 条、6 个健康官方来源；Facebook 社交 API 仍未授权 |
| X 浏览器真实导入与模型 | 用户已登录 Chrome；读取 8 条公开帖，云端回读原文、链接、精确时间、互动指标和重点标记均匹配。实际 Codex 调用生成 9 月 5 日（4 来源、2 报道）和 9 月 6 日（1 来源、1 报道）日报，引用与窗口校验通过；API 凭据状态仍明确待授权 |
| X 真实数据去重 | 同一 8 条帖子再次导入返回 `accepted=0, received=8`，没有重复入库 |
| X API 内容完整性契约 | 新增长帖全文、带引用的简短更新、主帖作者/时间/指标保留、引用不可用时的部分响应、空视频和无关回复检查；这些是按官方字段构造的模拟接口测试，不能替代真实 Token 联调 |
| X 官方 API 真实接入 | 用户提供 Token 并确认充值；加密部署到服务器，服务进程加载验证通过。用量接口 200；首轮采集 30 条，新增 6 条、更新 1 条已有记录；X 为 healthy，40 条总记录中包含 14 条 X 内容 |
| X 官方 API 内容回读 | 核验 7 条由 API 更新的记录（其中 6 条新增）：真实原文 URL、时间、整数互动指标、长帖及 Sam 引用 Jakub 的作者/时间上下文均保留。本次是实际 API 结果，区别于接口模拟测试 |
| X 凭据部署隔离 | 仅增加 X_BEARER_TOKEN，并将服务器查询限制改为每页 10 条、1 页；其他环境配置、34 条已有数据、开启的调度器均保留。HTTPS 200、未授权 401、reader 管理操作 403，Caddy 及现有 8080 服务正常 |
| X 官方 API → 中文日报 | 实际 Codex 任务约 25 秒完成 9 月 6 日回放：5 条输入合并为 1 条 Meta AIRA₃ 报道，引用 4 条官方 X 帖子；引用 ID 与窗口逐项核验，正式竞赛、赛后评估及内部基准的表述明确区分。更晚的 Jakub/Sam 帖子保留在尚未结束的 9 月 7 日窗口 |
| Android 真实 X 内容 | 原生 APK 回读新日报，X 来源筛选显示真实帖子和完整点赞数；详情保留原文与 X 永久链接，未发现致命运行错误。APK 仍为上述已交付版本，没有为本次数据导入修改应用代码 |

模型 API 的 OpenAI、Chat Completions、Anthropic 分支通过模拟响应测试；**只有 Codex 分支已用真实账号调用**。Claude Code CLI 与 Generic CLI 均实现适配，Generic CLI 已用真实子进程契约测试，Claude CLI 尚未完成真实授权联调。

审计曾发现 Expo 构建链 `xcode → uuid@7` 的中度公告 [GHSA-w5hq-g745-h8pq](https://github.com/advisories/GHSA-w5hq-g745-h8pq)。检查调用点仅使用 v4；仍将该依赖覆写到保留 CommonJS 的修复版 `11.1.1`，重新生成原生工程成功，复查为零已知漏洞。没有为消除提示而任意跨代升级全部依赖。

## 尚未完成，不能视为已交付

1. **关联网页全文补译**：数据库全量 47 份正文为 13 ready、34 review_required、0 error；最新公网默认近七天可见 43 个资源为 13 ready、30 review_required。当前 `07015b7` 仅升级通用校验代码，沿用分阶段推理配置，本次没有模型调用或重试待审内容。此前 `4d196af` 下正式单条任务 `fafcf476` 完成后由 error 转为待审；未新增整篇 ready，未通过候选没有发布，全文中文尚未补齐。
2. **Facebook 实际采集**：账号正在审核，后续 Graph API 还需具体应用权限与 Page ID。X 官方 API 已接通，且已观察到真实定时任务完成；当前分页上限不保证全面覆盖。
3. **Android 用户设备复验**：用户已反馈旧版 App 的实际使用问题；0.5.0 已交付签名 APK 并通过原生模拟器验证，尚无该最终版在用户手机上的复验结果。应用商店发布未请求，不作为本次 APK 交付的前置条件。
4. **iOS 运行与真机 IPA**：0.5.0 模拟器产物仍存在且已校验，但本机只有 Command Line Tools，没有完整 Xcode 或模拟器运行时；有效签名身份与描述文件均为 0，未生成 IPA。Apple 下载登录访问被自动审批审查拒绝，用户访问授权请求待回复；免费 Apple Account 即可获取兼容本机的 Xcode，不必等待付费会员才能运行模拟器。现有 EAS `preview` 的 Ad Hoc 真机分发另需付费开发者资格和目标设备 UDID，账号状态尚未确认；不会将商店发布强加为前置。旧 EAS 远程模拟器可用性查询仅保留为历史，没有重复查询。产物与操作见 [IOS.md](IOS.md)。

Tibo 默认采用已核验有 AI 产品相关动态的 `tibo_maker`；这是可在 App 中调整的关注配置，不作为构建或授权的阻碍。

## 当前 Android 交付产物

- `dist/ai-radar-0.5.0-android.apk`，69,622,350 字节；SHA-256 `142bb1f7bf3779df2f11e617fb57fff0f3f143e5677b80a3f60ad265834387ba`。
- 版本 `0.5.0` / `6`，原签名证书，包含一次网站许可与前台自动补采；当前版本的实际原生验收见本文 `0.5.0` 部分与 [ANDROID.md](ANDROID.md)。后续仅升级服务器翻译流程，没有重建移动端。

## 历史 Android 0.1.0 产物

- 文件：`dist/ai-radar-0.1.0-android.apk`（68,515,641 字节），校验文件位于同目录。
- SHA-256：`473a360d55c4b00c4b62059dfad9563d6f3b7049d690ee8eaa8ba962c54f0524`。
- 包名 `cn.yswdra.airadar`，版本 `0.1.0` / `1`。构建方式与签名备份见 [ANDROID.md](ANDROID.md)。
- 原生测试截图和日志保存在本机 Git 忽略的 `dist/android-smoke/`，不包含登录令牌。

## 本地查看

- App 预览：`http://localhost:8082`，提供明确标识的设计示例入口。
- 开发 API：`http://127.0.0.1:18473`，使用 `server/.env` 中的令牌。
- 当前 Chrome 预览另接 `127.0.0.1:18474` 的本机测试实例，使用非生产的临时 reader token，管理操作禁用。生产部署不会使用该测试令牌。
- 这些本地进程只在本机运行期间可用；启动命令见 README。

后续以 X/Facebook 的实际接入、iOS 原生构建与真机检查为准；上述进展不代表整项跨平台目标已完成。
