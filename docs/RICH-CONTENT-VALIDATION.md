# 2026-09-18 阅读、对话与 Mac 补采交付

- 服务端实现提交 `68d33c2` 已部署。源码清单 SHA256 `3bb56cd31096f0fc35800f7ef52a79da48fd20e6b07b6ab93fc202d9e525ce33`，源码和 protected 文件全部通过复核；未覆盖既有服务或端口。
- 主 API 与新 `ai-radar-chat` 独立运行；其他八个服务部署期间 PID 不变，最终十个服务均 active，`/healthz` 200。
- 真实 Sol medium 对话执行了系统状态查询、指定目录下临时文件写入/读取/删除、服务重启权限查询。未实际重启服务或改动文章。随后同一会话 resume 成功读取操作文档。首次验收发现新 docs 目录缺少组读取/遍历权限，已修复并通过真实续聊确认。
- 权限回归：普通阅读令牌不能访问管理员 Chat；Mac 专用凭据可取待采队列（真实返回三篇 OpenAI 页面），访问 Chat 返回 401。补采重复提交复用已有内容与处理任务。
- 服务端相关 165 项测试通过，Ruff 通过；App 71 项测试通过，另新增一项验证“等待验证的域名不阻塞其他域名”的 Mac 队列回归，与原有 Mac 策略测试一并通过。浏览器集成测试覆盖超过 50,000 元素的页面外壳、可见登录/验证码拦截、隐藏字段排除与不读取 Cookie。
- Markdown 的标题、加粗、列表、引用、表格、代码、链接和公式已用实际浏览器检查；原始 HTML/危险协议不可执行，阅读不会请求外部图片。完整原文和译文没有被开发者改写。
- Android stable 内容版本 **0.13.0.11**，runtime **0.13.0**，update `bcb75f93-72d7-49ee-a449-0e7e9802442c`，release `982f77ff-9816-4974-9fae-ba37568516e6`。原生指纹不变；preview/stable 公网清单、签名及全部资源哈希验证通过。模拟器完成下载、应用、重启，设置页显示 0.13.0.11，既有连接保留；阅读令牌下正确提示 Chat 需要管理权限。未生成新基础 APK。
- iOS 共用渲染代码已更新；没有新增 iOS 原生构建或发布未经验证的 iOS OTA。

## 尚需用户的一次操作

Mac 扩展已构建在本机 `dist/mac-companion`，带有仅供该服务器补采使用的私有凭据，禁止发布或分享。服务器接口已启用，但交付验收时尚未收到实际扩展心跳，因此不能声称 Mac 自动采集已上线。浏览器工具的 URL 安全策略禁止访问 `chrome://extensions`，不能通过另一种自动化方式绕过；需用户手动加载并点击「授权网站并启动」。操作见 [MAC-COLLECTOR.md](MAC-COLLECTOR.md)。

Mac 仍依赖电脑和 Chrome 在线；网站可再次要求验证，特殊 PDF 查看器也可能无法提取。超过 60,000 字时明确保留部分正文标志。原有服务器采集不依赖 Mac 在线，摘要与翻译继续由服务端正常流程执行。
